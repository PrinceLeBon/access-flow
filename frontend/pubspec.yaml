name: accessflow_frontend
publish_to: 'none'
description: AccessFlow – Request Management & Approval System (Flutter Web)
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  dio: ^5.4.0
  hydrated_bloc: ^9.1.2
  equatable: ^2.0.5
  json_annotation: ^4.8.1
  intl: ^0.18.1
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.6
  json_serializable: ^6.7.1

flutter:
  uses-material-design: true
